# Structure
Each Ansible Playbook should be a seperate YAML file instrumented via a centralized YAML to run.

# Compatibility
Only optimize for Ubuntu 26.04 systems or newer for bleeding edge hardware.