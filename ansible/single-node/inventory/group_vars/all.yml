---
# =============================================================================
# group_vars/all.yml — Single-Node
#
# All single-node playbooks are now self-contained with inline vars.
# This file is intentionally kept as a placeholder for future shared vars.
# =============================================================================

# (no vars defined — all tracks define their vars inline in their own playbooks)
